---
phase: 01.2-investigate-vad-triggered-translation-playback-before-ptt-release-during-speech-pauses
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/main.py
  - tests/unit/test_main.py
  - SCENARIOS.md
autonomous: true
requirements:
  - VAD-GATE-02
  - VAD-GATE-01
must_haves:
  truths:
    - "Operator can override playback-during-recording mode for a single launch using CLI flags."
    - "CLI override affects runtime behavior only and does not mutate config.yaml defaults."
    - "Default profile behavior remains playback-during-recording OFF unless explicitly enabled."
  artifacts:
    - path: "src/app/main.py"
      provides: "CLI parse + runtime-only override wiring for playback_during_recording"
      contains: "parse_cli_args"
    - path: "tests/unit/test_main.py"
      provides: "Regression tests for new boolean CLI option and non-persistence semantics"
      contains: "playback_during_recording"
    - path: "SCENARIOS.md"
      provides: "Operator-facing mode description aligned with speaker/headset behavior"
      contains: "playback_during_recording"
  key_links:
    - from: "src/app/main.py"
      to: "app.core.config.load_settings"
      via: "settings object mutation after profile load"
      pattern: "load_settings\(profile_override"
    - from: "src/app/main.py"
      to: "settings.audio.playback_during_recording"
      via: "runtime-only CLI override application"
      pattern: "playback_during_recording"
    - from: "tests/unit/test_main.py"
      to: "src/app/main.py"
      via: "argparse + handoff assertions"
      pattern: "--playback-during-recording|BooleanOptionalAction"
---

<objective>
Expose the existing playback mode through CLI runtime override, keeping default OFF and preserving config-file baseline behavior.

Purpose: Make early playback explicitly user-controlled per launch, matching locked decisions without introducing a new mode concept.
Output: Updated CLI surface in `main.py`, unit tests for override semantics, and aligned scenario documentation.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-investigate-vad-triggered-translation-playback-before-ptt-release-during-speech-pauses/01.2-CONTEXT.md
@.planning/phases/01.2-investigate-vad-triggered-translation-playback-before-ptt-release-during-speech-pauses/01.2-RESEARCH.md
@.planning/phases/01-cli-profile-flag/01-02-SUMMARY.md
@src/app/main.py
@tests/unit/test_main.py
@SCENARIOS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime-only CLI boolean override for existing playback mode</name>
  <files>src/app/main.py</files>
  <action>Extend `parse_cli_args()` with `--playback-during-recording` / `--no-playback-during-recording` using `argparse.BooleanOptionalAction` and `default=None` to preserve profile defaults when unset. In startup wiring, apply override only to the in-memory `settings.audio.playback_during_recording` after `load_settings(...)` returns. Do not write back to YAML and do not alter current profile resolution precedence.</action>
  <verify>uv run pytest tests/unit/test_main.py -k "playback_during_recording or parse_cli_args or profile"</verify>
  <done>CLI can opt into or out of early playback per run, and unset flag behavior continues to honor profile config defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests proving CLI override and non-persistence behavior</name>
  <files>tests/unit/test_main.py</files>
  <action>Add tests that cover both flag states and default `None` behavior, and verify the override mutates only runtime settings handed to startup components. Include a check that running the CLI path does not modify `config.yaml` content. Reuse existing main-entrypoint test patterns (monkeypatching heavy startup services) to keep tests fast and deterministic.</action>
  <verify>uv run pytest tests/unit/test_main.py</verify>
  <done>Unit tests fail if CLI override is not applied correctly or if config persistence side effects are introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Align scenario docs with default-off and optional headset-mode override</name>
  <files>SCENARIOS.md, src/app/main.py, tests/unit/test_main.py</files>
  <action>Update `SCENARIOS.md` language so speaker mode (`playback_during_recording: false`) is clearly the default and headset mode remains opt-in. Align CLI help text wording with documentation terminology to reduce operator confusion. Finish by running ruff and basedpyright on changed Python files and fix any issues without suppression comments.</action>
  <verify>uv run ruff check src/app/main.py tests/unit/test_main.py && uv run basedpyright src/app/main.py tests/unit/test_main.py</verify>
  <done>Docs, CLI help text, and tests all consistently describe runtime-only override with default OFF semantics.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_main.py` passes with coverage for `--playback-during-recording`, `--no-playback-during-recording`, and default-unset behavior.
- Runtime launch applies CLI override in memory only; profile YAML remains unchanged.
- `SCENARIOS.md` and CLI help text both describe the same speaker/headset mode expectations.
- `uv run ruff check ...` and `uv run basedpyright ...` pass for modified Python files.
</verification>

<success_criteria>
- VAD-GATE-02: Existing mode is configurable via both profile config and CLI override, with CLI applied per launch only.
- VAD-GATE-01: Default behavior remains OFF unless explicitly enabled by config value or CLI flag.
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-investigate-vad-triggered-translation-playback-before-ptt-release-during-speech-pauses/01.2-02-SUMMARY.md`
</output>
