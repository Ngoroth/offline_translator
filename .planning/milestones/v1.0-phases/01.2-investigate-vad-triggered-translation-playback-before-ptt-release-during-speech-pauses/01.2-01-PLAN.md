---
phase: 01.2-investigate-vad-triggered-translation-playback-before-ptt-release-during-speech-pauses
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/orchestrator/pipeline.py
  - tests/integration/test_pipeline_vad.py
  - tests/unit/test_player_barge_in.py
autonomous: true
requirements:
  - VAD-GATE-01
  - VAD-GATE-03
must_haves:
  truths:
    - "When playback-during-recording is disabled, translated audio does not play while PTT is still held."
    - "When playback-during-recording is enabled, playback can start during LISTENING when VAD harvest produces chunks."
    - "Releasing PTT still flushes queued translated chunks in original FIFO order."
  artifacts:
    - path: "src/app/orchestrator/pipeline.py"
      provides: "Playback consumption gate in player worker keyed by settings and session state"
      contains: "_player_worker"
    - path: "tests/integration/test_pipeline_vad.py"
      provides: "Hold-vs-release playback timing regression coverage"
      contains: "playback_during_recording"
    - path: "tests/unit/test_player_barge_in.py"
      provides: "Worker-level guardrail for queue handling while listening"
      contains: "SessionState.LISTENING"
  key_links:
    - from: "src/app/orchestrator/pipeline.py"
      to: "settings.audio.playback_during_recording"
      via: "player worker decision before AudioPlayer.play"
      pattern: "playback_during_recording"
    - from: "src/app/orchestrator/pipeline.py"
      to: "app.orchestrator.session.SessionState"
      via: "LISTENING-state defer path"
      pattern: "SessionState\.LISTENING"
    - from: "tests/integration/test_pipeline_vad.py"
      to: "src/app/orchestrator/pipeline.py"
      via: "integration assertions on pre-release vs post-release playback"
      pattern: "handle_input_complete|_player_worker"
---

<objective>
Make playback timing honor the existing `audio.playback_during_recording` mode so early playback is opt-in instead of always-on during VAD-triggered speech pauses.

Purpose: Close the urgent runtime gap while preserving existing VAD pause harvesting, queue ordering, and resume-speech behavior.
Output: A playback gate in `TranslationPipeline` plus focused tests that lock hold/release playback behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-investigate-vad-triggered-translation-playback-before-ptt-release-during-speech-pauses/01.2-CONTEXT.md
@.planning/phases/01.2-investigate-vad-triggered-translation-playback-before-ptt-release-during-speech-pauses/01.2-RESEARCH.md
@.planning/phases/01.1-fix-windows-audio-backend-selection-for-desktop-profile/01.1-01-SUMMARY.md
@src/app/orchestrator/pipeline.py
@tests/integration/test_pipeline_vad.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate player worker output during LISTENING using existing playback mode</name>
  <files>src/app/orchestrator/pipeline.py</files>
  <action>Update `_player_worker()` so it checks `self.settings.audio.playback_during_recording` before calling audio playback while session state is `LISTENING`. If the mode is disabled, defer playback until release transitions state away from `LISTENING`, without dropping queued chunks or changing FIFO order. Do not alter VAD harvest thresholds, pause-delay defaults, or any new overlap/duck policy; this phase is only a playback-consumption gate.</action>
  <verify>uv run pytest tests/integration/test_pipeline_vad.py -k "playback or hold or release"</verify>
  <done>Player worker enforces opt-in early playback and preserves current segment production and queue ordering semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for disabled-by-default hold behavior and enabled early-playback path</name>
  <files>tests/integration/test_pipeline_vad.py, tests/unit/test_player_barge_in.py</files>
  <action>Add or update tests to assert that with `playback_during_recording=False`, no translated chunk is played before PTT release even if VAD already produced it, and that with `playback_during_recording=True`, playback may start while still holding. Include an assertion that release still flushes pending queue items in original order. Keep existing barge-in semantics unchanged; only tighten assertions around timing gate behavior.</action>
  <verify>uv run pytest tests/integration/test_pipeline_vad.py tests/unit/test_player_barge_in.py</verify>
  <done>Automated tests fail if early playback occurs when mode is off, and pass for both opt-in and release-flush scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Run mandatory quality gates for touched runtime and tests</name>
  <files>src/app/orchestrator/pipeline.py, tests/integration/test_pipeline_vad.py, tests/unit/test_player_barge_in.py</files>
  <action>Run targeted lint and type checks after test updates and fix all findings without adding suppression comments. Ensure typing remains strict in modified code paths and test doubles.</action>
  <verify>uv run ruff check src/app/orchestrator/pipeline.py tests/integration/test_pipeline_vad.py tests/unit/test_player_barge_in.py && uv run basedpyright src/app/orchestrator/pipeline.py tests/integration/test_pipeline_vad.py tests/unit/test_player_barge_in.py</verify>
  <done>Changed files have zero ruff/basedpyright issues and behavior tests remain green.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/integration/test_pipeline_vad.py tests/unit/test_player_barge_in.py` passes with explicit assertions for hold-vs-release playback timing.
- With mode off, player calls are deferred until release; with mode on, early playback during LISTENING is allowed.
- `uv run ruff check ...` and `uv run basedpyright ...` pass for all touched files.
</verification>

<success_criteria>
- VAD-GATE-01: Existing `audio.playback_during_recording` becomes authoritative for whether playback can occur before PTT release.
- VAD-GATE-03: Pause timing defaults and resume-speech runtime policy remain unchanged while queue order stays stable.
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-investigate-vad-triggered-translation-playback-before-ptt-release-during-speech-pauses/01.2-01-SUMMARY.md`
</output>
