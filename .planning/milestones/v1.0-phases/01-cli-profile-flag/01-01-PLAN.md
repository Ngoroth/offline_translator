---
phase: 01-cli-profile-flag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/core/config.py
  - tests/unit/test_settings.py
autonomous: true
requirements:
  - CLI-02
  - CLI-03
must_haves:
  truths:
    - "Runtime-selected profile overrides config current_profile for that run only"
    - "Unknown profile names fail fast with actionable error output"
  artifacts:
    - path: "src/app/core/config.py"
      provides: "Profile override precedence and profile existence validation"
      contains: "load_settings(config_path, profile_override=...)"
    - path: "tests/unit/test_settings.py"
      provides: "Regression coverage for profile override and invalid profile errors"
      contains: "profile_override"
  key_links:
    - from: "load_settings(profile_override=...)"
      to: "RootConfig.profiles"
      via: "membership validation before AppSettings.model_validate"
      pattern: "if active_profile not in root.profiles"
---

<objective>
Implement profile override semantics in the config layer so runtime profile selection does not require editing YAML.

Purpose: Keep profile resolution and validation in one source of truth while enforcing fail-fast behavior for invalid profile names.
Output: Updated config loader behavior plus unit tests for override precedence and invalid-profile messaging.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-profile-flag/01-CONTEXT.md
@.planning/phases/01-cli-profile-flag/01-RESEARCH.md
@src/app/core/config.py
@tests/unit/test_settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add profile override parameter to config loading</name>
  <files>src/app/core/config.py</files>
  <action>Extend load_settings to accept optional profile_override: str | None while keeping existing config_path behavior unchanged. Resolve active profile with override precedence (override when provided, otherwise root.current_profile), validate exact case-sensitive key membership in root.profiles, and keep runtime-only behavior (never write back to config.yaml). Raise ValueError for unknown profile names including the invalid name and available profile keys to satisfy user-facing correction UX.</action>
  <verify>uv run pytest tests/unit/test_settings.py</verify>
  <done>load_settings supports runtime override without breaking existing callers, and invalid override names produce a clear ValueError with available profiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for override and invalid profile paths</name>
  <files>tests/unit/test_settings.py</files>
  <action>Add tests that build a temporary multi-profile config and assert (1) profile_override selects the requested profile even when current_profile differs, (2) config file content is unchanged after loading with override, and (3) nonexistent override raises ValueError containing invalid name plus available profile names.</action>
  <verify>uv run pytest tests/unit/test_settings.py -k "profile or override"</verify>
  <done>Unit tests fail before implementation and pass after implementation, proving override precedence and invalid-profile handling.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce local quality gate for changed code</name>
  <files>src/app/core/config.py, tests/unit/test_settings.py</files>
  <action>Run lint and type checks after code changes and adjust implementation/types until both are clean without adding lint or type suppression comments.</action>
  <verify>uv run ruff check src/app/core/config.py tests/unit/test_settings.py && uv run basedpyright src/app/core/config.py tests/unit/test_settings.py</verify>
  <done>Ruff and basedpyright report zero issues on modified files.</done>
</task>

</tasks>

<verification>
- Running `uv run pytest tests/unit/test_settings.py` passes with new override-path coverage.
- Passing `profile_override` selects requested profile data while leaving YAML file bytes unchanged.
- Unknown profile names return actionable ValueError content and stop startup path.
</verification>

<success_criteria>
- CLI-02 behavior is implemented in config loading (`profile_override` wins over `current_profile` in-memory).
- CLI-03 behavior is implemented with clear invalid-profile errors containing available options.
- Existing non-override load_settings call sites continue working unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-profile-flag/01-01-SUMMARY.md`
</output>
