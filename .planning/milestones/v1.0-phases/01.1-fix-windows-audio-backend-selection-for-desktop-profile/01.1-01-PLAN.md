---
phase: 01.1-fix-windows-audio-backend-selection-for-desktop-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/core/audio/recorder_sounddevice.py
  - src/app/core/audio/recorder_selector.py
  - src/app/core/audio/__init__.py
  - src/app/main.py
  - tests/unit/test_audio.py
  - tests/unit/test_main.py
autonomous: true
requirements:
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
must_haves:
  truths:
    - "Desktop profile launches use a Windows-compatible recorder backend instead of Linux-only arecord"
    - "Raspberry Pi profile launches continue to use existing ALSA arecord recorder behavior"
    - "Recorder backend selection follows active profile intent via settings.platform for deterministic --profile runs"
    - "Recorder startup failures remain actionable with backend-relevant AudioDeviceError guidance"
  artifacts:
    - path: "src/app/core/audio/recorder_selector.py"
      provides: "Single source of recorder backend selection from platform intent"
      contains: "select_recorder_factory"
    - path: "src/app/core/audio/recorder_sounddevice.py"
      provides: "Windows-compatible recorder implementation using sounddevice InputStream"
      contains: "class SoundDeviceAudioRecorder"
    - path: "src/app/main.py"
      provides: "Startup wiring that chooses recorder backend after device resolution"
      contains: "create_audio_recorder"
    - path: "tests/unit/test_main.py"
      provides: "Regression tests for profile-driven recorder selection wiring"
      contains: "settings.platform"
  key_links:
    - from: "src/app/main.py"
      to: "src/app/core/audio/recorder_selector.py"
      via: "recorder factory chosen from settings.platform"
      pattern: "select_recorder_factory\(settings\.platform\)"
    - from: "src/app/core/audio/recorder_selector.py"
      to: "src/app/core/audio/recorder.py"
      via: "linux/rpi mapping to ALSA recorder"
      pattern: "return AudioRecorder"
    - from: "src/app/core/audio/recorder_selector.py"
      to: "src/app/core/audio/recorder_sounddevice.py"
      via: "windows mapping to sounddevice recorder"
      pattern: "return SoundDeviceAudioRecorder"
---

<objective>
Implement profile-driven recorder backend selection so Windows desktop profile startup uses sounddevice while Raspberry Pi profile startup keeps ALSA arecord behavior.

Purpose: Fix urgent Windows startup failure without broad audio refactors by centralizing backend selection in a small, testable selector keyed by active settings.
Output: New sounddevice recorder backend, selector wiring in main startup path, and regression tests covering desktop vs Pi backend choice.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-fix-windows-audio-backend-selection-for-desktop-profile/01.1-CONTEXT.md
@.planning/phases/01.1-fix-windows-audio-backend-selection-for-desktop-profile/01.1-RESEARCH.md
@.planning/phases/01-cli-profile-flag/01-02-SUMMARY.md
@src/app/main.py
@src/app/core/audio/recorder.py
@tests/unit/test_audio.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Windows-compatible recorder backend with shared recorder contract</name>
  <files>src/app/core/audio/recorder_sounddevice.py, src/app/core/audio/__init__.py</files>
  <action>Create a sounddevice-based recorder implementation for input capture that matches the existing recorder surface used by the pipeline (`start`, `stop`, `get_chunk`, `get_last_chunk`, `extract_buffer`). Use `sd.InputStream` with float32 mono capture and support numeric device indices from resolved settings. Wrap startup/runtime failures in `AudioDeviceError` with backend-specific guidance, and keep ALSA recorder untouched. Export the new recorder class from the audio package without removing current exports.</action>
  <verify>uv run pytest tests/unit/test_audio.py -k "audio_recorder or AudioDeviceError"</verify>
  <done>Windows-compatible recorder backend exists, can be instantiated with resolved input device indices, and preserves actionable error semantics via AudioDeviceError.</done>
</task>

<task type="auto">
  <name>Task 2: Add profile-driven recorder selector and wire startup recorder construction</name>
  <files>src/app/core/audio/recorder_selector.py, src/app/main.py</files>
  <action>Add a small selector/factory keyed by `settings.platform` that maps `windows` to sounddevice recorder and `linux`/`rpi` to existing ALSA recorder. Treat profile intent as authoritative and use host OS fallback only for `auto`. Update `main.py` recorder creation to call this selector after existing device resolution flow, keeping device resolution logic and audio player path unchanged. Ensure desktop profile path cannot hit arecord startup when `settings.platform` is windows.</action>
  <verify>uv run pytest tests/unit/test_main.py -k "profile or platform or recorder"</verify>
  <done>main startup uses selected recorder backend from settings.platform, preserves existing Pi/ALSA behavior, and keeps current device resolution integration unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend-selection regression tests and enforce quality gates</name>
  <files>tests/unit/test_main.py, tests/unit/test_audio.py, src/app/core/audio/recorder_selector.py, src/app/core/audio/recorder_sounddevice.py, src/app/main.py</files>
  <action>Extend tests to cover backend selection outcomes for desktop vs rpi profile intent, including deterministic behavior for explicit `settings.platform` values and fallback behavior for `auto`. Preserve existing ALSA error-path coverage and add focused assertions that desktop wiring does not depend on arecord. Finish by running ruff and basedpyright on all touched files and fix all findings without suppression comments.</action>
  <verify>uv run pytest tests/unit/test_main.py tests/unit/test_audio.py && uv run ruff check src/app/main.py src/app/core/audio/recorder_selector.py src/app/core/audio/recorder_sounddevice.py src/app/core/audio/__init__.py tests/unit/test_main.py tests/unit/test_audio.py && uv run basedpyright src/app/main.py src/app/core/audio/recorder_selector.py src/app/core/audio/recorder_sounddevice.py src/app/core/audio/__init__.py tests/unit/test_main.py tests/unit/test_audio.py</verify>
  <done>Automated tests validate backend selection and existing ALSA regressions remain green, with zero lint/type issues on changed files.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_main.py tests/unit/test_audio.py` passes with explicit coverage for windows vs rpi recorder backend selection.
- Desktop-profile startup path is wired to sounddevice recorder construction and no longer depends on arecord availability.
- Pi-profile startup path continues to use ALSA recorder behavior with existing arecord error-path expectations intact.
- `uv run ruff check ...` and `uv run basedpyright ...` return zero issues on modified files.
</verification>

<success_criteria>
- AUDIO-01: Recorder backend selection is centralized and driven by `settings.platform` profile intent.
- AUDIO-02: Desktop profile startup uses sounddevice recorder path and avoids Linux-only arecord requirement.
- AUDIO-03: Raspberry Pi profile path remains ALSA/arecord-based with actionable startup error guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-fix-windows-audio-backend-selection-for-desktop-profile/01.1-01-SUMMARY.md`
</output>
