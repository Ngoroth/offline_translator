---
phase: 01-cli-profile-flag
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/main.py
  - tests/unit/test_main.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
must_haves:
  truths:
    - "User can start the app with --profile <name> to select an existing config profile"
    - "CLI-selected profile is visible at startup so operators know what ran"
    - "Invalid profile input exits non-zero and surfaces a clear correction path"
  artifacts:
    - path: "src/app/main.py"
      provides: "CLI parsing and startup path integration for --profile"
      contains: "argparse.ArgumentParser(allow_abbrev=False)"
    - path: "tests/unit/test_main.py"
      provides: "Unit tests for CLI parsing and profile handoff behavior"
      contains: "--profile"
  key_links:
    - from: "src/app/main.py"
      to: "src/app/core/config.py"
      via: "load_settings(profile_override=args.profile)"
      pattern: "load_settings\(.*profile_override"
    - from: "argparse parsed args"
      to: "startup logging"
      via: "active profile line on successful launch"
      pattern: "Profile:"
---

<objective>
Wire the CLI entrypoint to accept `--profile`, pass it into config loading, and make active-profile startup output explicit.

Purpose: Enable one-command profile switching per run while preserving default behavior when no flag is supplied.
Output: Updated `main.py` CLI path and unit tests for argument parsing/integration behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-profile-flag/01-CONTEXT.md
@.planning/phases/01-cli-profile-flag/01-RESEARCH.md
@.planning/phases/01-cli-profile-flag/01-01-PLAN.md
@src/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add non-abbreviated CLI parsing for profile selection</name>
  <files>src/app/main.py</files>
  <action>Introduce argparse-based argument parsing (allow_abbrev=False) with optional --profile NAME flag and clear help text. Keep behavior unchanged when the flag is omitted by passing None to config loading. Ensure the existing async bootstrap flow remains intact.</action>
  <verify>uv run python src/app/main.py --help</verify>
  <done>CLI help shows --profile, abbreviated flags are not accepted, and launching without --profile still follows existing defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate parsed profile into settings load and startup visibility</name>
  <files>src/app/main.py</files>
  <action>Pass parsed args.profile into load_settings(profile_override=...) and include the active profile key in startup output on successful launch. Preserve fail-fast behavior for invalid profiles from config loader so process exits non-zero with clear message content from Task 01-01.</action>
  <verify>uv run pytest tests/unit/test_main.py -k "profile"</verify>
  <done>main.py forwards CLI profile selection to config loading and startup logs clearly indicate the active profile key.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused unit tests for CLI wiring and run quality checks</name>
  <files>tests/unit/test_main.py, src/app/main.py</files>
  <action>Create/extend unit tests that validate parse behavior and integration handoff (with and without --profile), including propagation of invalid-profile failures to non-zero process behavior. Finish with lint/type checks on touched files without suppression comments.</action>
  <verify>uv run pytest tests/unit/test_main.py && uv run ruff check src/app/main.py tests/unit/test_main.py && uv run basedpyright src/app/main.py tests/unit/test_main.py</verify>
  <done>CLI profile path is covered by tests and all lint/type checks pass on modified files.</done>
</task>

</tasks>

<verification>
- `uv run python src/app/main.py --help` documents `--profile` usage.
- `uv run pytest tests/unit/test_main.py` verifies profile handoff and default behavior.
- Invalid profile launches surface clear errors and non-zero exit behavior via loader validation.
</verification>

<success_criteria>
- CLI-01 is met: users can pass `--profile <name>` at launch.
- CLI-02 remains true end-to-end: runtime profile selection overrides `current_profile` without file mutation.
- CLI-03 remains true at CLI entrypoint: unknown profile errors are visible and actionable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-profile-flag/01-02-SUMMARY.md`
</output>
