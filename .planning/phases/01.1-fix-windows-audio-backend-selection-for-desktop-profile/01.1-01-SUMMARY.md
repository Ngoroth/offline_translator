---
phase: 01.1-fix-windows-audio-backend-selection-for-desktop-profile
plan: 01
subsystem: audio
tags: [python, sounddevice, alsa, profile-selection, windows]

# Dependency graph
requires:
  - phase: 01-cli-profile-flag
    provides: deterministic profile override loading via --profile
provides:
  - Windows profile recorder routing to sounddevice InputStream backend
  - Centralized recorder backend selector keyed by settings.platform
  - Regression tests for explicit profile mapping and auto host fallback
affects: [startup, audio-capture, desktop-profile, rpi-profile]

# Tech tracking
tech-stack:
  added: []
  patterns: [profile-intent-first recorder selection, backend-specific AudioDeviceError guidance]

key-files:
  created: [src/app/core/audio/recorder_sounddevice.py, src/app/core/audio/recorder_selector.py]
  modified: [src/app/main.py, src/app/core/audio/__init__.py, tests/unit/test_main.py, tests/unit/test_audio.py]

key-decisions:
  - "Recorder backend selection now keys on settings.platform with host fallback used only when platform is auto."
  - "Sounddevice startup/runtime failures are wrapped in AudioDeviceError so startup guidance remains actionable per backend."

patterns-established:
  - "Selection pattern: choose recorder class via select_recorder_factory(settings.platform) after device resolution."
  - "Compatibility pattern: keep ALSA recorder untouched for linux/rpi while adding Windows-specific recorder implementation."

requirements-completed: [AUDIO-01, AUDIO-02, AUDIO-03]

# Metrics
duration: 4 min
completed: 2026-02-21
---

# Phase 01.1 Plan 01: Windows Audio Backend Selection Summary

**Desktop-profile startup now routes recorder creation to a new sounddevice backend while rpi/linux profiles remain on the ALSA arecord recorder path.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-21T08:37:57Z
- **Completed:** 2026-02-21T08:41:42Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments
- Added `SoundDeviceAudioRecorder` with `start/stop/get_chunk/get_last_chunk/extract_buffer` API parity for Windows-compatible capture.
- Added `select_recorder_factory(settings.platform)` so recorder backend selection is centralized and deterministic for explicit profiles.
- Added regression coverage for windows/linux/rpi/auto selection behavior and startup wiring, then passed pytest + ruff + basedpyright gates on touched files.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add Windows-compatible recorder backend with shared recorder contract** - `3c767a4` (feat)
2. **Task 2: Add profile-driven recorder selector and wire startup recorder construction** - `246757c` (feat)
3. **Task 3: Add backend-selection regression tests and enforce quality gates** - `d39c850` (test)

**Plan metadata:** pending final docs commit.

## Files Created/Modified
- `src/app/core/audio/recorder_sounddevice.py` - New sounddevice-based recorder backend using `sd.InputStream` and `AudioDeviceError` wrapping.
- `src/app/core/audio/recorder_selector.py` - Platform-intent selector mapping windows to sounddevice and linux/rpi to ALSA recorder.
- `src/app/main.py` - Startup wiring now selects recorder factory from `settings.platform` after device resolution.
- `src/app/core/audio/__init__.py` - Exported `SoundDeviceAudioRecorder`.
- `tests/unit/test_main.py` - Added selector mapping, auto fallback, and recorder-construction wiring tests.
- `tests/unit/test_audio.py` - Added sounddevice startup error-wrapping regression test.

## Decisions Made
- Used a dedicated selector module (`recorder_selector.py`) as the single source of recorder backend dispatch.
- Kept host OS detection only for `platform: auto` fallback so explicit profile intent remains authoritative.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Guarded sounddevice recorder construction without active event loop**
- **Found during:** Task 3 (quality gate test execution)
- **Issue:** Creating `SoundDeviceAudioRecorder` in a synchronous context raised runtime errors before startup.
- **Fix:** Made loop capture optional and guarded async queue scheduling to run only when an event loop is active.
- **Files modified:** `src/app/core/audio/recorder_sounddevice.py`
- **Verification:** `uv run pytest tests/unit/test_main.py tests/unit/test_audio.py`
- **Committed in:** `d39c850`

**2. [Rule 1 - Bug] Improved sounddevice error classification for PortAudio startup failures**
- **Found during:** Task 3 (new recorder error-path test)
- **Issue:** PortAudio failures wrapped as generic recorder errors when exception type was `RuntimeError`.
- **Fix:** Extended PortAudio detection to inspect exception text and preserve backend-specific AudioDeviceError messaging.
- **Files modified:** `src/app/core/audio/recorder_sounddevice.py`
- **Verification:** `uv run pytest tests/unit/test_main.py tests/unit/test_audio.py`
- **Committed in:** `d39c850`

---

**Total deviations:** 2 auto-fixed (2 bug fixes)
**Impact on plan:** Deviations were scoped, correctness-focused fixes required to satisfy recorder startup reliability and error guidance criteria.

## Issues Encountered
- `basedpyright` initially rejected passing a recorder factory union into pipeline wiring; resolved by narrowing `main.py` handoff with an explicit cast and by tightening new test stubs to meet strict type checks.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 01.1 plan is complete and validated with tests/lint/type checks.
- Ready to continue with Phase 2 deployment planning/execution.

---
*Phase: 01.1-fix-windows-audio-backend-selection-for-desktop-profile*
*Completed: 2026-02-21*

## Self-Check: PASSED
