---
phase: 02-deploy-to-pi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy.py
  - tests/unit/test_deploy.py
  - README.md
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05
user_setup:
  - service: raspberry-pi-ssh
    why: "Deployment requires direct SSH access to the target host"
    env_vars: []
    dashboard_config:
      - task: "Ensure `pi@translator` resolves from the developer machine and accepts SSH authentication"
        location: "Local SSH configuration / network DNS or hosts"
must_haves:
  truths:
    - "User runs `uv run scripts/deploy.py` and the project syncs to `pi@translator` without manual copy commands."
    - "Deployment excludes `.venv/`, `__pycache__/`, `.git/`, and `logs/` from transfer output."
    - "After sync, dependencies are installed on Pi automatically via `uv sync` in the deployed directory."
    - "Deploy output shows stage progress and ends with clear success or concise actionable failure guidance."
  artifacts:
    - path: "scripts/deploy.py"
      provides: "Stage-based deployment orchestration (preflight -> rsync -> remote uv sync)"
      contains: "def main"
    - path: "tests/unit/test_deploy.py"
      provides: "Regression tests for command construction, excludes, stage ordering, and failure mapping"
      contains: "deploy"
    - path: "README.md"
      provides: "Operator instructions for deploy preconditions and command usage"
      contains: "Deploy to Raspberry Pi"
  key_links:
    - from: "scripts/deploy.py"
      to: "pi@translator"
      via: "ssh preflight and remote command execution"
      pattern: "ssh.*pi@translator"
    - from: "scripts/deploy.py"
      to: "rsync"
      via: "sync command with required excludes and progress"
      pattern: "--exclude=.venv/|--exclude=__pycache__/|--exclude=.git/|--exclude=logs/"
    - from: "scripts/deploy.py"
      to: "remote uv sync"
      via: "post-sync SSH command in deploy directory"
      pattern: "cd .* && uv sync"
---

<objective>
Deliver one-command Raspberry Pi deployment that reliably syncs code, installs dependencies remotely, and reports actionable status.

Purpose: Remove manual deploy steps so Windows-to-Pi iteration stays fast and repeatable.
Output: A production-ready `scripts/deploy.py`, unit tests for deploy behavior, and clear operator documentation in README.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deploy-to-pi/02-CONTEXT.md
@.planning/phases/02-deploy-to-pi/02-RESEARCH.md
@README.md
@scripts/
@tests/unit/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deploy preflight and staged command orchestration</name>
  <files>scripts/deploy.py</files>
  <action>Create `scripts/deploy.py` as a stage-based pipeline: `preflight -> rsync -> remote uv sync`. Use explicit command arrays with `subprocess.run(..., check=True, text=True)` (no `shell=True`). In preflight, validate local `ssh` and `rsync` availability plus remote reachability at `pi@translator`; if remote `uv` is missing, surface concise remediation guidance (1-2 concrete next-step commands) without treating it as an upfront baseline requirement. Keep failures concise and actionable with one root cause plus next steps, per locked decision. Choose one canonical remote path constant (for example `/home/pi/offline_translator`) and use it consistently in sync and remote install stages.</action>
  <verify>uv run python scripts/deploy.py --help</verify>
  <done>Script has deterministic staged flow with explicit preflight checks for tooling, SSH target, and remote uv readiness before file transfer starts.</done>
</task>

<task type="auto">
  <name>Task 2: Add rsync excludes, progress output, and post-sync uv install flow</name>
  <files>scripts/deploy.py</files>
  <action>Implement rsync command targeting `pi@translator` with required excludes: `.venv/`, `__pycache__/`, `.git/`, `logs/`. Use canonical source/destination trailing-slash semantics to avoid nested remote directories. Enable visible transfer progress and stage banners so operators can track deploy state. After successful rsync, run `ssh pi@translator "cd <remote_dir> && uv sync"`. Map subprocess failures (especially SSH/255) to concise actionable guidance and end with a single success/failure summary line.</action>
  <verify>uv run pytest tests/unit/test_deploy.py -k "exclude or rsync or uv_sync or ssh"</verify>
  <done>Running deploy performs sync with required exclusions, then executes remote `uv sync`, and output clearly communicates progress and final status.</done>
</task>

<task type="auto">
  <name>Task 3: Add deploy regression tests and operator usage docs, then run quality gates</name>
  <files>tests/unit/test_deploy.py, README.md, scripts/deploy.py</files>
  <action>Add unit tests that assert command construction (including exclude flags and destination host), stage order, and error-to-guidance mapping for SSH failures and missing remote `uv`. Keep tests deterministic by mocking subprocess execution. Update README with a "Deploy to Raspberry Pi" section covering the SSH-only baseline precondition, the single deploy command, and expected stage output including remediation guidance if `uv` is missing on the Pi. Finish with required validation on touched files: pytest for deploy tests, ruff, and basedpyright; fix all issues without ignore comments.</action>
  <verify>uv run pytest tests/unit/test_deploy.py && uv run ruff check scripts/deploy.py tests/unit/test_deploy.py && uv run basedpyright scripts/deploy.py tests/unit/test_deploy.py</verify>
  <done>Deploy behavior is protected by tests, docs explain preparation/usage, and lint/type checks pass cleanly on modified Python files.</done>
</task>

</tasks>

<verification>
- `uv run scripts/deploy.py` reaches preflight, sync, and remote install stages with clear progress labels.
- Sync command excludes `.venv/`, `__pycache__/`, `.git/`, and `logs/` exactly.
- Remote install runs only after successful rsync and executes `uv sync` in the chosen deploy directory.
- SSH/connectivity failures return concise actionable guidance instead of raw traceback-only output.
- `uv run pytest tests/unit/test_deploy.py`, `uv run ruff check ...`, and `uv run basedpyright ...` pass.
</verification>

<success_criteria>
- DEPLOY-01: User can deploy with `uv run scripts/deploy.py` without manual copy/install steps.
- DEPLOY-02: Required directories are excluded from transfer by deploy command construction and tests.
- DEPLOY-03: Remote dependency installation (`uv sync`) runs automatically after successful sync.
- DEPLOY-04: Script explicitly targets and verifies `pi@translator` for SSH operations.
- DEPLOY-05: Deploy output provides stage progress and clear success/failure status with actionable remediation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deploy-to-pi/02-01-SUMMARY.md`
</output>
