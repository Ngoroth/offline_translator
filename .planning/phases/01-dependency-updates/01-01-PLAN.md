---
phase: 01-dependency-updates
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
autonomous: true
requirements:
  - DEPS-01
  - DEPS-02
  - DEPS-03
  - DEPS-04

must_haves:
  truths:
    - "llama-cpp-python is at version 0.3.16"
    - "piper-tts is at version 1.4.1"
    - "pytest-asyncio is at version 1.3.0"
    - "All 128 tests pass with updated dependencies"
    - "No new linting errors introduced"
    - "No new type checking errors introduced"
    - "Pipeline runs without errors with updated packages"
  artifacts:
    - path: "pyproject.toml"
      provides: "Updated dependency version constraints"
      contains: ["llama-cpp-python>=0.3.16", "piper-tts>=1.4.1", "pytest-asyncio>=1.3.0"]
    - path: "uv.lock"
      provides: "Locked dependency versions"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv sync --extra dev"
      pattern: "uv sync"
    - from: "installed packages"
      to: "test suite"
      via: "uv run pytest"
      pattern: "pytest"
---

<objective>
Update core dependencies to latest stable versions with verified compatibility.

Purpose: Foundation update before reliability improvements. Catches any breaking changes early while codebase is unchanged.
Output: Updated pyproject.toml, synced uv.lock, verified test suite.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md

@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dependency versions in pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
    Update the following dependency version constraints in pyproject.toml:

    **Core dependencies (line ~9):**
    - `llama-cpp-python>=0.3.8` → `llama-cpp-python>=0.3.16`
    - `piper-tts>=1.3.0` → `piper-tts>=1.4.1`
    - `sounddevice>=0.5.3` → `sounddevice>=0.5.5`

    **Dev dependencies (line ~24):**
    - `pytest-asyncio>=0.23.0` → `pytest-asyncio>=1.3.0`

    Do NOT modify other dependencies or configuration settings.
    Preserve all existing formatting and structure.
  </action>
  <verify>
    grep -E "llama-cpp-python>=0.3.16|piper-tts>=1.4.1|pytest-asyncio>=1.3.0|sounddevice>=0.5.5" pyproject.toml | wc -l
    # Expected: 4 matching lines
  </verify>
  <done>
    pyproject.toml contains updated version constraints for all four target packages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync dependencies and run test suite</name>
  <files>uv.lock</files>
  <action>
    Sync dependencies with updated versions, then run the full test suite.

    **Step 1:** Run `uv sync --extra dev` to update uv.lock and install packages.
    This may take several minutes as llama-cpp-python and piper-tts download/build.

    **Step 2:** Run `uv run pytest -v --tb=short` to execute all 128 tests.
    If any tests fail, analyze the failure output:
    - If pytest-asyncio related: Check for asyncio_mode compatibility
    - If llama-cpp-python related: Check for API changes in 0.3.16
    - If piper-tts related: Check for voice/config changes in 1.4.1

    **Expected:** All 128 tests pass. Test count should match existing count.
  </action>
  <verify>
    uv run pytest --co -q | grep -c "test session starts\|tests collected" || uv run pytest -v 2>&1 | tail -20
    # Look for: "X passed" where X >= 128
  </verify>
  <done>
    uv.lock is updated with new package versions.
    All 128 tests pass without errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify linting and type checking</name>
  <files></files>
  <action>
    Run code quality checks to ensure no new errors introduced by dependency updates.

    **Step 1:** Run `uv run ruff check .` for linting.
    Expected: "All checks passed!" or equivalent success message.

    **Step 2:** Run `uv run basedpyright` for type checking.
    Expected: "0 errors, 4 warnings" (warnings are acceptable as per PROJECT.md).

    If new errors appear:
    - Analyze if they're related to updated packages
    - Check for type stub changes in updated packages
    - Report findings before making changes (may need separate fix)
  </action>
  <verify>
    uv run ruff check . 2>&1 | grep -c "All checks passed"
    uv run basedpyright 2>&1 | grep "0 errors"
    # Both should return non-zero counts (success)
  </verify>
  <done>
    Ruff reports all checks passed.
    Basedpyright reports 0 errors (warnings acceptable).
    No new linting or type checking errors introduced.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run a final validation:

```bash
# 1. Verify installed versions
uv pip list | grep -E "llama-cpp-python|piper-tts|pytest-asyncio|sounddevice"

# 2. Quick smoke test that imports work
uv run python -c "import llama_cpp; import piper; import pytest_asyncio; import sounddevice; print('All imports successful')"

# 3. Re-run tests one more time to confirm stability
uv run pytest -x -q
```
</verification>

<success_criteria>
1. **DEPS-01**: llama-cpp-python is at version 0.3.16 or higher
2. **DEPS-02**: piper-tts is at version 1.4.1 or higher
3. **DEPS-03**: pytest-asyncio is at version 1.3.0 or higher
4. **DEPS-04**: All 128 existing tests pass after dependency updates
5. Pipeline runs without errors with updated packages (imports succeed)
6. No new linting errors (ruff check passes)
7. No new type checking errors (basedpyright reports 0 errors)
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-updates/01-01-SUMMARY.md` documenting:
- Updated package versions installed
- Test results (passed count)
- Any issues encountered and resolutions
- Confirmation that success criteria are met
</output>
